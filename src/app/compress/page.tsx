import ImageCompressPage from '@/components/tools/ImageCompress'

export const metadata = {
  title: 'Image Compressor — Reduce Image Size | FileForge',
  description: 'Compress JPG, PNG, and WEBP images without losing quality. Batch compress up to 20 images at once.',
}

export default function CompressPage() {
  return <ImageCompressPage />
}
