import Base64ToolPage from '@/components/tools/Base64Tool'

export const metadata = {
  title: 'Base64 Encoder/Decoder | FileForge',
  description: 'Encode files to Base64 strings or decode Base64 back to files. Free, fast, and private.',
}

export default function Base64Page() {
  return <Base64ToolPage />
}
