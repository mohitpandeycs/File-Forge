import ToolsGrid from "@/components/tools/ToolsGrid";

export const metadata = {
  title: "All Conversion Tools — FileForge",
  description:
    "Browse all available file conversion tools for documents, spreadsheets, images, and utilities.",
};

export default function ToolsPage() {
  return <ToolsGrid />;
}
