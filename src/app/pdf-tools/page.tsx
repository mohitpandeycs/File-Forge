import PDFToolsPage from '@/components/tools/PDFTools'

export const metadata = {
  title: 'PDF Tools — Merge, Split & Compress | FileForge',
  description: 'Merge multiple PDFs, split pages, and compress PDF files — all in your browser. No upload needed.',
}

export default function PDFTools() {
  return <PDFToolsPage />
}
